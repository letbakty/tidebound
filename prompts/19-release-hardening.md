# Этап 19 — Стабилизация до беты

**Модель:** Opus 5 или Fable 5 (поиск и починка сквозных проблем).
**Зависит от:** всё. **Читать:** docs/00 §16–17.
**Ресерч:** [research/24-testing-headless-hardening.md](../research/24-testing-headless-hardening.md) — **`assert` вырезается в release**, свой раннер, soak-тест с CSV, память и орфаны, санитария сигналов, CI.
**Плюс, если созданы к этому моменту** (⏳ файл будет создан ресерч-агентом по брифу): `research/36-*` краш-репортинг и обратная связь от игроков ([BRIEF-next](../research/BRIEF-next.md)), `research/38-*` юридический минимум (лицензии шрифтов и звуков в титрах, политика приватности, возрастные рейтинги), `research/46-*` фиксация версии движка для воспроизводимой сборки ([BRIEF-next3](../research/BRIEF-next3.md)). Плюс [research/26-build-size-and-optimization.md](../research/26-build-size-and-optimization.md) — размер билда, strip, рантайм-оптимизация, чек-лист релизной части и «что НЕ делать». Дополнительно: [research/11](../research/11-sim-core-determinism.md) §10 (grep-аудит).
**Готовый код:** `research/code/audit_sim.sh` → `tools/audit_sim.sh` — переноси, а не пиши с нуля (черновики под контракты docs/02, но не компилировались: сверь имена полей с прошлыми этапами).

## Задача
1. **Автопилотный стресс:** headless-прогон 20 забегов разными сидами (скрипт tests/soak.gd; готовая схема с CSV — [research/24](../research/24-testing-headless-hardening.md) §6): ни одного краша/ошибки; собери статистику (очки, выжившие, причины смертей) в CSV — это заготовка баланс-данных.
2. **Санитария детерминизма:** повтори хеш-тест после КАЖДОЙ системы. Grep-аудит — **перенеси готовый `research/code/audit_sim.sh` в `tools/`** (проверяет все шесть источников недетерминизма из [research/11](../research/11-sim-core-determinism.md) §1, не только `randf`): глобальный RNG, `Time.*`, нестабильные сортировки, float-накопители, итерация по словарям, нодовые API в `sim/`.
3. **Санитария сигналов:** скрипт-проверка: каждый сигнал Events кем-то эмитится и кем-то слушается (мёртвые — в отчёт); сигнатуры обработчиков совпадают (в debug-сборке прогон с проверкой).
4. **Память:** 3 забега подряд без выхода — снапшоты памяти, утечки (незакрытые View при run_ended, копящиеся тосты, орфаны нод — `print_orphan_nodes`).
5. **Краевые случаи:** загрузка битого сейва (обрезанный JSON) — мягкий отказ; сейв в момент смерти агента; выход во время драфта; пауза во время подъёма воды; окно 800×600 и 3840×2160.
6. **Локализация:** прогон обоих языков — нет ключей «сырых» (`KEY_NAME` на экране), нет вылезающего за панели текста (RU длиннее EN на ~20%).
6a. ⚠️ **Раннер обязан валить прогон при `SCRIPT ERROR`, а не только при провале `check()`.** В проекте уже был случай: 365 ошибок за прогон при полностью зелёном отчёте, из-за чего материалы станций молча не списывались. Пока раннер их не видит, любой дефект такого класса невидим.
6b. **Оставшиеся находки ревью** — `review/05-project-and-release.md` (REL-02 профиль обнуляется при смене версии, REL-03 не сохраняется при выходе, REL-05 `.translation` в git, REL-06 незафиксированные настройки, REL-07 `print` в релизе, REL-08 автостарт забега в `main.gd`) и `review/06-tests-gaps.md` (TEST-04 санитария сигналов, TEST-05 golden-master, TEST-06 точность хеша, TEST-07 устойчивость к порядку обработчиков).
7. **Релизная часть** по [research/26](../research/26-build-size-and-optimization.md) §2–3, §7: настройки экспорта уровня 0 (ничего не стоят), strip и упаковка дистрибутива, замер итогового размера билда. ⚠️ UPX не применять (§4), список «что НЕ делать» — §8.
8. TODO-инвентаризация: собери все `TODO` по проекту в docs/backlog.md с приоритетами.

## Приёмка
- [ ] 20/20 soak-забегов без ошибок; CSV лежит в docs/.
- [ ] Нулевой grep-аудит sim/; отчёт по сигналам чист.
- [ ] Все краевые случаи из списка обработаны явно.
