# 48 — Steam API: что можно добавить сверх базовой интеграции

**Для этапов:** 20 (Steam), 11 (поля отчёта — решается сейчас), 15 (экраны), 18 (фото-режим), плюс продуктовые решения по расширению.
**Дата ресерча:** 2026-08-22. **Движок:** Godot 4.7.x + GodotSteam.
Дополняет [27-steam-integration.md](27-steam-integration.md), где закрыта база: GodotSteam, архитектура `platform.gd`, достижения, Cloud, Deck, страница и демо. Здесь — **всё остальное, чего там нет.**

---

## 0. Главное за 30 секунд

Четыре возможности, которые дают проекту не «галочку в списке фич», а реальную ценность:

| # | Что | Почему именно нам |
|---|---|---|
| **1** | **Timeline API** | Игра построена на драматичных секундах: вода вернулась, двое не успели. Timeline ставит маркеры прямо в запись Steam — игрок вырезает клип одним кликом. Это усиливает главный маркетинговый хук проекта |
| **2** | **Aggregated Stats** | Бесплатная телеметрия баланса со всех игроков без своего сервера. Прямо закрывает половину темы 37 из `BRIEF-next.md` |
| **3** | **Leaderboards + 64 int32 на запись** | Это не только очки: в каждую запись влезает **весь профиль забега**. Соревнование и распределения данных одним механизмом |
| **4** | **Steam Playtest** | Заменяет самодельную процедуру закрытой беты на 20 человек: без ключей, без почты, без влияния на отзывы и вишлисты |

---

## 1. Полная карта интерфейсов: что применимо

| Интерфейс | Нам | Комментарий |
|---|---|---|
| `ISteamUserStats` | ✅ **ядро** | Достижения, статы, лидерборды, глобальные агрегаты |
| `ISteamTimeline` | ✅ **сильная фича** | Маркеры и клипы — см. §2 |
| `ISteamRemoteStorage` | ✅ | Cloud, покрыт в 27 §3 |
| `ISteamFriends` | ✅ дёшево | Rich Presence — см. §5 |
| `ISteamScreenshots` | ✅ потом | Фото-режим из плана расширения |
| `ISteamInput` | ✅ | Deck и геймпады, покрыт в 27 §4.1 |
| `ISteamApps` | ✅ | Проверка DLC и языка установки |
| `ISteamUtils` | ✅ | Оверлей, батарея Deck, состояние сети |
| `ISteamUGC` | ❌ | Workshop и моды — в списке «не входит никогда» |
| `ISteamMatchmaking`, `ISteamNetworking*`, `ISteamGameServer*`, `ISteamParties` | ❌ | Игра соло |
| `ISteamInventory` | ❌ | Нет предметов для торговли, и не будет |
| `ISteamRemotePlay` | ❌ | Together — для кооп-игр |
| `ISteamMusic`, `ISteamVideo`, `ISteamHTTP` | ❌ | Не наш случай |

---

## 2. Timeline API — главная находка

**Что это.** Часть Steam Game Recording. Работает и без разработчика, но игра может размечать запись: события с иконкой, заголовком, описанием и **приоритетом клипа**. Игрок открывает таймлайн записи и видит не сплошную ленту, а помеченные моменты.

**Почему для нас это больше, чем для других.** В `atlas-nish-2026` главным аргументом за концепт было: у игры **встроенный клип-генератор**, один цикл прилива = один готовый ролик. Timeline превращает это из свойства геймплея в свойство продукта: игроку не надо искать момент в записи — он уже помечен.

**Что размечать** (всё это уже есть как события в `Events`):

| Событие | Приоритет | Иконка |
|---|---|---|
| Агент утонул | **Featured** | смерть |
| Найдена реликвия | Featured | достижение |
| Шторм начался | Featured | — |
| Сизигия | Standard | — |
| Приход существ | Standard | — |
| Постройка сломана | Standard | — |
| Судно пришло, забег окончен | Featured | — |
| Отзыв с людьми ниже нуля | Standard | тревога |

Иконок Valve даёт около 50 штук с префиксом `steam_`, плюс числовые `steam_0`…`steam_99` и возможность загрузить свои.

**Сверено по заголовкам SDK 1.65** (`public/steam/isteamtimeline.h`, интерфейс `STEAMTIMELINE_INTERFACE_V004`) — веб-источники по этой теме врут, ниже факты из кода:

```
SetTimelineGameMode( ETimelineGameMode )          // Playing / Staging / Menus / LoadingScreen
AddInstantaneousTimelineEvent( title, description, icon, iconPriority,
                               startOffsetSeconds, clipPriority ) -> handle
AddRangeTimelineEvent( ... , duration, clipPriority ) -> handle
StartRangeTimelineEvent / UpdateRangeTimelineEvent / EndRangeTimelineEvent
RemoveTimelineEvent( handle )
SetTimelineTooltip( description, timeDelta ) / ClearTimelineTooltip
OpenOverlayToTimelineEvent( handle )
```

⚠️ **Поправка к более раннему утверждению:** `SetTimelineGameMode` **не удалён** — он есть в 1.65 и нужен, чтобы Steam отличал игру от меню и загрузки. Утверждение об удалении в 1.61 взято из веб-пересказа и оказалось неверным.

Приоритеты клипа: `None` (1) · `Standard` (2) · `Featured` (3).

### Game Phases — забег как единица записи

Отдельный механизм того же интерфейса, которого нет ни в одном обзоре:

```
StartGamePhase() / EndGamePhase()
SetGamePhaseID( phaseID )
AddGamePhaseTag( tagName, tagIcon, tagGroup, priority )
SetGamePhaseAttribute( attributeGroup, attributeValue, priority )
DoesGamePhaseRecordingExist( phaseID ) / OpenOverlayToGamePhase( phaseID )
```

Фаза группирует кусок записи и вешает на него **теги и атрибуты**, которые Steam показывает игроку в оверлее.

Для нас это ложится один в один: **фаза = забег**. `phaseID` — сид. Теги — выбранные карты вылазки. Атрибуты — утёс, число циклов, исход, финальные очки. Игрок открывает запись и видит не безымянный кусок, а «сид 4242 · 12 циклов · судно · 41 очко», и может перейти прямо к нему.

Это сильнее отдельных событий: события помечают секунды, фазы дают структуру всей записи. Приоритет управляет тем, что покажут первым при нехватке места.

**Цена:** подписка на уже существующие сигналы плюс таблица «событие → иконка, приоритет». Логика в `platform.gd`, `sim/` не трогается. Для фаз — `run_started` / `run_ended`, данные уже есть в отчёте забега.

---

## 3. Aggregated Stats — телеметрия без сервера

**Как работает.** Стат помечается «aggregated» в админке Steamworks → Steam копит глобальную сумму по всем игрокам → игра запрашивает `RequestGlobalStats(days)` и читает `GetGlobalStat`, а историю по дням — `GetGlobalStatHistory`.

Сверено по `isteamuserstats.h` (SDK 1.65): `RequestGlobalStats( int nHistoryDays )`, `GetGlobalStat` в двух вариантах — `int64` и `double`, `GetGlobalStatHistory` тоже в двух. Плюс рядом `RequestGlobalAchievementPercentages()` — процент игроков, открывших каждое достижение: даровой индикатор того, до какого места доходит средний игрок.

**Что это даёт нам.** `BRIEF-next.md` тема 37 ставила задачу «телеметрия без своего сервера». Steam закрывает её наполовину бесплатно.

Что стоит завести статами:

```
runs_started · runs_won_ship · runs_wiped · runs_left_early
deaths_drown · deaths_storm · cycles_survived_total
relics_found · deepest_mark_reached · cards_picked_<id>
```

Из соотношений сразу видно то, чего не покажет ни один локальный soak: **какова реальная доля вайпов у живых игроков**. Сейчас автопилот даёт 20 побед из 20, и главный вопрос баланса — так ли это у людей.

⚠️ **Ограничение.** Агрегаты дают суммы, а не распределения: «сколько всего смертей от утопления» узнаешь, «на каком цикле обычно умирают» — нет. Распределения — через лидерборды (§4).

⚠️ **Защита от накрутки обязательна.** Статы лежат у пользователя и подделываются. В админке задать min, max, «только увеличение» и **max change** — последнее ограничивает, насколько быстро один читер сдвинет глобальную сумму.

---

## 4. Лидерборды: соревнование и данные одним механизмом

**Ключевой факт:** к каждой записи прикрепляется **до 64 значений int32** (`k_cLeaderboardDetailsMax = 64`, подтверждено по `isteamuserstats.h` в SDK 1.65; имя лидерборда — до 128 символов). Это не «очки и всё» — это место под весь профиль забега: цикл выхода, выжившие, глубина, потери по причинам, выбранные карты, использованные разблокировки.

То есть один механизм даёт сразу и таблицу рекордов, и **распределения** для балансировки, которых не дают агрегаты.

Лимит — 10 000 лидербордов на игру, так что схема может быть подробной: общий по очкам · по каждому утёсу · по длине забега (12/16/20) · **дневной**.

**Дневной забег.** В плане расширения он стоит под номером 9 (`audit/02-expansion.md` §5). Steam даёт под него инфраструктуру бесплатно: один сид на всех, одна попытка, лидерборд дня. Классика жанра — так делают рогалайты со времён TumbleSeed, и это ровно тот «ритуал», который возвращает игрока ежедневно.

У нас для этого уже всё есть: детерминированная симуляция, сид забега, готовый подсчёт очков. Не хватает только UI и самого лидерборда.

---

## 5. Дешёвое, но заметное

**Rich Presence.** Друзья видят не «играет в Tidebound», а «Цикл 7 из 12 · ярус −8 · шторм». Это буквально пара строк через `steam_display` и словарь локализованных шаблонов. Дёшево, работает на узнаваемость и на любопытство в списке друзей.

**Screenshots API.** Понадобится, когда дойдём до фото-режима (расширение §5 п.10): снимок уходит прямо в библиотеку Steam с привязкой к игре.

**Steam Playtest.** Отдельное бесплатное приложение: игрок жмёт «Запросить доступ», разработчик выдаёт. Не нужны ключи и списки почт, не влияет на отзывы и вишлисты основной страницы, размер теста регулируется. По плану выхода у нас закрытая бета на 20 человек — это готовая под неё инфраструктура, и она же дешевле, чем раздача ключей вручную.

**Оверлей и батарея.** `ISteamUtils` умеет отдавать заряд батареи Deck — можно мягко предупреждать, что до конца забега 90 минут, а батареи меньше.

---

## 6. Что НЕ брать

Workshop и UGC (моды в «не входит никогда») · всё сетевое, matchmaking и лобби (игра соло) · Inventory и торговлю предметами · Remote Play Together · Steam Music и Video. Trading Cards можно рассмотреть **после** релиза как маркетинг, но это не фича игры.

---

## 7. Порядок и цена

| Что | Когда | Цена | Что даёт |
|---|---|---|---|
| Поля отчёта под достижения и статы | **сейчас, этап 11/20** | S | Потом каждое поле = правка sim + сейва + миграции |
| Достижения | этап 20 | S | База, покрыта в 27 |
| Aggregated stats | этап 20 | S | Телеметрия баланса с живых игроков |
| Rich Presence | этап 20 | XS | Видимость у друзей |
| Timeline | этап 20 или сразу после | S–M | Усиление главного хука проекта |
| Playtest | перед бетой | XS | Замена самодельной раздачи ключей |
| Лидерборды (общий) | к релизу | M | Соревнование + распределения |
| Дневной забег | 1.0 | M | Ежедневный возврат игрока |
| Screenshots | вместе с фото-режимом | S | — |

---

## Чек-лист

- [ ] Поля отчёта забега заведены **до** первых игроков (иначе миграция сейва).
- [ ] Все статы, которые нужны для баланса, помечены aggregated в админке, с min/max и max change.
- [ ] Таблица «событие sim → Timeline-маркер» лежит в `platform.gd`, а не в `sim/`.
- [ ] Схема лидербордов продумана до первого релиза: переименовать потом нельзя, а лимит 10 000 позволяет не экономить.
- [ ] Playtest заведён отдельным приложением до рассылки бете.
- [ ] Ни один вызов Steam не попал в `sim/` — иначе headless-тесты и Android-сборка сломаются.

## Источники

- [Steamworks API Overview](https://partner.steamgames.com/doc/sdk/api) — полный список интерфейсов
- [Steam Timeline](https://partner.steamgames.com/doc/features/timeline) — события, приоритеты клипов, иконки
- [Stats and Achievements](https://partner.steamgames.com/doc/features/achievements) — aggregated stats, `RequestGlobalStats`, `GetGlobalStatHistory`, защита от накрутки
- [Step by Step: Stats](https://partner.steamgames.com/doc/features/achievements/stats_guide) — типы INT/FLOAT/AVGRATE
- [Steam Leaderboards](https://partner.steamgames.com/doc/features/leaderboards) — 10 000 лидербордов, 64 int32 на запись
- [Testing on Steam](https://partner.steamgames.com/doc/store/testing) — Playtest
- [Enhanced Rich Presence](https://partner.steamgames.com/doc/features/enhancedrichpresence)
- [GodotSteam](https://godotsteam.com/) — changelog, поддержка SDK 1.61+

## 8. GodotSteam: покрытие, лицензия, куда слать патчи

**Сверено по исходникам** (клон ветки `gdextension` с Codeberg, сопоставление с `steam_api.json` из SDK 1.65):

| | |
|---|---|
| Методов в SDK | **921** в 34 интерфейсах |
| Забиндено в GodotSteam | **742** — около **81%** |
| `ISteamTimeline` | **18/18 — полностью**, включая Game Phases и `setTimelineGameMode` |
| `ISteamUserStats` | глобальные статы и лидерборды на месте |
| `ISteamScreenshots` | 9/9 |
| `ISteamUGC` | 95/99 |

**Не покрыто то, чего нам и не надо:** `ISteamClient` (1/33 — низкоуровневый доступ к пайпам), колбэк-интерфейсы матчмейкинга (`ISteamMatchmaking*Response`, 0%), `ISteamParentalSettings`, `ISteamMusic` (1/9), `ISteamNetworkingFakeUDPPort`. Ни одно из этого в соло-игре не нужно.

**Вывод:** всё, что перечислено в этом файле как возможность, **уже обёрнуто**. Оговорку из ранней версии («Timeline может быть обёрнут не целиком, тогда Game Phases отложить») снимаем — обёрнут целиком.

### Лицензия и участие

- **Лицензия MIT** (`license.md`, «GP Garcia, Chris Ridenour, and Contributors») — использовать и модифицировать можно свободно.
- ⚠️ **Репозиторий на GitHub архивирован** (июль 2026, 3737 звёзд, 0 открытых issue). Разработка переехала на **[codeberg.org/godotsteam/godotsteam](https://codeberg.org/godotsteam/godotsteam)**: 24 открытых issue, обновление 19.08.2026, pull requests включены. Идти с патчем на GitHub бессмысленно — там архив.
- Веток `main`/`master` нет, ветки по типам сборки: `gdextension`, `godot4`, `godot3`, `gdnative`, `*-plugin`. Для нас — **`gdextension`**.
- **SDK в репозиторий не входит** — в `godotsteam/sdk/` лежит файл-заглушка «Include Steam SDK here». Наш `vendor/steamworks-headers/` (SDK 1.65) закрывает это, но для сборки понадобятся ещё и бинарники из оригинального архива (`redistributable_bin/`).

**Нужно ли нам что-то туда слать.** По текущему плану — нет: всё нужное обёрнуто. Патч понадобится, только если в новом SDK появится метод, который нам нужен раньше, чем его добавят мейнтейнеры. Тогда путь такой: форк на Codeberg → ветка от `gdextension` → правка в `godotsteam.cpp` (биндинг + метод) и `godotsteam.h` → pull request. Формат виден по соседним методам: `ClassDB::bind_method(D_METHOD("имяКамелКейсом", "аргументы"), &Steam::метод)`.

---

✅ **Проверено по исходникам.** Локальная копия **Steamworks SDK 1.65** (`steamworks_sdk_165.zip`) — все сигнатуры Timeline, Game Phases, глобальных статов и лимитов лидерборда взяты из заголовков `public/steam/*.h`, а не из веб-пересказов. Один из таких пересказов уже оказался неверным (про удаление `SetTimelineGameMode`), поэтому **сверяться нужно с заголовками**.

⚠️ Остаётся непроверенным одно: какие из этих методов уже обёрнуты в **GodotSteam**. Заголовки SDK говорят, что API есть; наличие биндинга проверить по changelog GodotSteam перед реализацией. Если Timeline не обёрнут целиком — Game Phases можно отложить, события важнее.
